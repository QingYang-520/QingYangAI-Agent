# 青阳AI 项目交接文档

> 交接日期：2026-09-12（持续更新，最近一次：2026-09-21——新增「提交快照」约定：`Z:\git提交\年-月-日\rc{N}\` 存工程源码副本，提交前必做）
> 最后版本：1.9.18-rc2（versionCode 105，2026-09-20）
> 技术栈：.NET MAUI / C# / net10.0-android（另含 net10.0-windows）

---

## 一、软件方向（现在是什么 / 想成为什么）

**一句话**：青阳AI 是一个运行在 Android 上的**多模态 AI 助手 + 系统终端能力**的聊天应用。

### 核心功能
1. **AI 聊天**（文本对话模型，默认 DeepSeek，可换 GLM 等任意 OpenAI 兼容接口）
   - 流式输出、思考过程显示（可折叠）、自动上下文压缩、AI 性格系统
   - 缓存命中率统计、上下文占用显示
2. **终端命令能力**（通过 Shizuku 执行系统命令，AI 输出 `{cmd:"..."}` 触发）
   - 三行终端头部气泡、高危命令确认、终端结果回传上下文
3. **多模态**（设置页 5 组模型配置，3 套快照可切换）
   - 文本对话 / 文生图 / 听觉(语音转文字) / 视觉(图片理解) / 文字转语音
   - 发图 → 视觉模型识别 → 结果进历史；`{img:"..."}` 指令触发文生图 + 全屏鉴赏
   - 语音输入（系统识别优先 → 听觉模型回退）
4. **AI 上网浏览**（AI 输出 `{browse:"url"}` 触发，后台 HttpClient 抓取网页正文回传给 AI，不是用户浏览器）
5. **每条消息 AI 必看**（独立开关，AI 每次私下复盘情绪/意图/潜台词/关系走向，注入下一轮 system）
6. **主题色自定义**（8 个预设色，默认紫色，动态应用到全界面）
7. **启动欢迎页**（WebView 内嵌 HTML 动画 + 立即体验按钮 → 用户协议 → 聊天页）

### 架构特点
- 单项目 MAUI（`青阳AI.csproj`），Android 为主
- Shizuku 绑定库在 **C:\shizuku-bind\Shizuku.Bindings**（独立项目引用）
- 配置存 `Preferences`（3 套配置快照 MC0/MC1/MC2，含旧版迁移）
- 聊天历史存本地文件（`chahis.qingyang`）
- 签名：青阳AI 自建 keystore（永久有效）

---

## 二、什么能动 / 什么不能动 / 能动但要注意

### ✅ 可以随便动
| 文件 | 说明 |
|------|------|
| `ChatPage.xaml(.cs)` | 聊天主界面，UI/逻辑随便改 |
| `SettingsPage.xaml(.cs)` | 设置页，可加配置项 |
| `AppSettings.cs` | 全局设置 + BasePersona（AI 人设）+ 性格系统 |
| `IntroPage.xaml(.cs)` | 启动欢迎页（内嵌 HTML） |
| `ShimmerLabel.cs` / `MorphIcon.cs` / `StatusBadge.cs` | 自定义控件 |
| `ImagePreviewPage.xaml(.cs)` | 全屏图片鉴赏 |
| `BrowserTool.cs` | AI 上网浏览工具（后台 HttpClient 抓网页，非 UI） |
| `MessageReviewService.cs` | 每条消息 AI 必看（复盘缓存，注入 system） |

### 🚫 不能动 / 动之前必须知道
| 项 | 原因 |
|----|------|
| **`ApplicationId`（com.qingyang.ai）** | 改了就装不上旧包，等于换应用 |
| **签名密钥**（keystore）| 丢了/换了签名 → 无法覆盖安装更新，用户必须卸载重装 |
| **`RootNamespace`（青阳AI）** | 全项目命名空间，动了全崩 |
| **Shizuku 绑定库路径** | 在 `C:\shizuku-bind`，删了 Android 目标编译失败 |
| **`AndroidPackageFormats`（apk）** | 改了可能影响 aab/apk 输出 |

### ⚠️ 能动但要注意
1. **项目路径已是纯 ASCII**（`D:\project\QIngYangAI`，2026-09-20 重命名完成）→ 可直接编译，**无需再复制到暂存目录**；目录名 `QIngYangAI`（I 大写）是 ZCode 工作区根目录，**勿再改名**（会触发"目录被占用"）
2. **`ChatPage` 里 `CompressImageAsync`**：视觉模型（SenseNova 商汤）只认小图，**512px + 质量60**，改大了会 socket closed
3. **`ImageSource` 等属性**：图片数据别直接序列化进历史文件，会爆
4. **思维链**：`Thinking`（思考过程）**不要发给模型**（token 爆），但 `TerminalExecLog`（终端执行记录）和 `VisionResult`（图片识别结果）**要发给模型**（BuildHistoryMessages 已处理）
5. **视觉识别**：`stream=true`（SSE 流式）+ 429 自动重试（每次重建 HttpRequestMessage，否则报 already_sent）
6. **Shizuku**：AndroidManifest 里有 Provider 声明 + 权限，别删

---

## 三、签名密钥

| 项 | 值 |
|----|-----|
| 密钥库文件 | `D:\project\QIngYangAI\keystore\qingyang.keystore`（csproj 内为相对路径 `keystore\qingyang.keystore`） |
| 别名 | `qingyangai` |
| 密钥库密码 | `********` |
| 密钥密码 | `********` |
| 证书 SHA-256 | `e818daf5960f6f17d24971e80f38d8124107f95deee488f6124dc12aa7e2a8fd` |

> ⚠️ **一定要备份 keystore 文件和这两个密码！** 丢了就再也无法更新这个应用。
> 密码已写在 `青阳AI.csproj` 的 `<AndroidSigningKeyStore>` / `<AndroidSigningKeyPass>` / `<AndroidSigningStorePass>` 属性（PropertyGroup 内），打包会自动用。

---

## 四、怎么打包（关键！）

### ✅ 2026-09-20 起：直接构建（暂存目录工作流已废弃）

目录重命名已完成（见下方「目录现状」），项目位于纯 ASCII 路径，**不再需要复制到暂存目录**，直接在官方工程构建：

```powershell
# 1)（可选）先关 build server，降低 XABAA7000/MSB6006 抖动概率
dotnet build-server shutdown

# 2) 直接 publish（在官方工程目录执行；原生 PowerShell，勿用 Git-Bash）
cd D:\project\QIngYangAI
dotnet publish -f net10.0-android -c Release `
  -p:AndroidSdkDirectory="D:\project\QIngYangAI\android-sdk" `
  -nodeReuse:false -p:UseSharedCompilation=false
```
产出：`D:\project\QIngYangAI\bin\Release\net10.0-android\com.qingyang.ai-Signed.apk`（约 60.4 MB）

### 复制产物（必做）
```powershell
# 3) 复制回项目 apk 目录（命名规则见第五节）+ 往 Z 盘根目录同名复制一份
Copy-Item "D:\project\QIngYangAI\bin\Release\net10.0-android\com.qingyang.ai-Signed.apk" `
  "D:\project\QIngYangAI\apk\青阳AI-1.9.18-rc2.apk"
Copy-Item "D:\project\QIngYangAI\bin\Release\net10.0-android\com.qingyang.ai-Signed.apk" `
  "Z:\青阳AI-1.9.18-rc2.apk"
```

### 提交快照（每次改完代码 / 打包前必做，2026-09-21 起）

工程**没有 git**，用「日期 + rc 序号」的目录快照代替 commit。**每次改完代码、打包之前必须做一次。**

- **位置**：`Z:\git提交\年-月-日\rc{当日序号}\`
  - 日期文件夹按**提交当天**命名，格式 `yyyy-MM-dd`（如 `2026-09-21`）
  - 里面每提交一次新建一个 `rc{N}`，**每天从 rc1 重新起算**
  - `rc{N}` 里放的是**工程源码副本**
- **一键执行**（原生 PowerShell 窗口）：
  ```powershell
  powershell -ExecutionPolicy Bypass -File "D:\project\QIngYangAI\git提交.ps1"
  ```
  脚本自动：算当天已用序号 → 建 `年-月-日\rc{N}` → 复制源码 → 汇报版本号 / 文件数 / 体积。
- **复制范围**：工程根下**除下列内容外全部复制**
  - 排除目录：`bin` `obj` `.vs` `android-sdk` `apk` `.workbuddy` `.workbuddy-ai` `.zcode` `node_modules` `dsh`
  - 排除文件：`*.user`
  - 实际内容 ≈ 源码 + `Platforms/` `Resources/` `Properties/` `libs/` `keystore/` `svg/` + csproj/slnx + 文档，约 **7.4 MB / 130 个文件**
- **注意**
  - APK **不进快照**——它单独归档到 `apk\` 和 `Z:\`（见上一步）
  - keystore **会进快照**（密钥本来就该多一份备份）
  - 脚本文件本身用 **UTF-8 BOM** 保存（PowerShell 5.1 按 ANSI 读会把中文读乱）
- **已有快照**：`Z:\git提交\2026-09-21\rc1`（对应 1.9.18-rc2）

### 上传到 GitHub（每次改完代码必做，2026-09-21 起）

仓库：**`QingYang-520/QingYangAI-Agent`** —— ⚠️ **公开仓库（public）**，分支 `main`

```powershell
cd D:\project\QIngYangAI
python _ghpush.py --go -m "这次改了什么"
```

脚本自动完成：生成脱敏 csproj → `git add -A` → 顶替暂存区敏感文件 → 安全自检 → commit + push。

**公开仓库脱敏规则（硬要求，别绕过）**

| 项 | 规则 |
|----|------|
| `青阳AI.csproj` | 上传前复制一份到 `csproj\青阳AI.csproj`，删掉**全部**签名配置（`AndroidKeyStore` / `AndroidSigningKeyStore` / `AndroidSigningKeyAlias` / `AndroidSigningKeyPass` / `AndroidSigningStorePass`）；仓库里存的是这份干净版，**本地原文件不动** |
| `交接文档.md` | 文档里写的 keystore 密码，上传时打码成 `********` |
| `keystore/` | **永不入库**（`.gitignore` 已挡 `keystore/` `*.keystore` `*.jks`） |
| 二进制 | `*.png` `*.ttf` `*.aar` `*.pptx` 不入库（体积大，不是"修改"） |
| 自检 | 提交前 `git write-tree` + `git grep` 扫密钥，扫到就中止、不推 |

- ⚠️ `.git/config` 里存着 GitHub 访问令牌（PAT）→ **`.git` 目录绝不能进任何快照或上传**（`git提交.ps1` 已排除）
- ⚠️ 仓库是公开的，历史提交 `e643e65`（9/15 那次）里仍留有旧 csproj 的**明文密码**，删不掉（除非重写历史）。
  所以那个 `qingyang.keystore` 文件更要看紧——密码已经不算秘密，密钥文件本身才是最后一道锁

### 常见坑
- **XABAA7000（Permission denied）/ MSB6006（java.exe exit 1）/ IL1011**：文件占用类抖动。先 `dotnet build-server shutdown`，删项目内 `bin`、`obj`，查残留 java/dotnet 进程，再重试；连续两次失败就彻底清一遍再来
- **MSB1008**：别在 publish 参数里拼 shell 片段（如 `; echo exit=$?`）
- **net_http_client_request_already_sent**：重试逻辑必须每次 new HttpRequestMessage（已修复）
- **MSYSTEM 陷阱**：必须在原生 PowerShell 构建——Git-Bash 注入 `MSYSTEM=MINGW64` 会让 NuGet 走 Unix 分支直接崩

### 目录现状（2026-09-20 重命名后）
- 官方工程：`D:\project\QIngYangAI`（2026-09-20 深夜已从嵌套目录**拍平到工作区根目录**；目录名 I 是大写、ZCode 固定路径，**勿再改名**）
- Android SDK：`D:\project\QIngYangAI\android-sdk`（build-tools 36.0.0 / platforms android-36）
- keystore：`D:\project\QIngYangAI\keystore\qingyang.keystore`（csproj 内已是相对路径，项目再移动也无需改）
- 残留目录已全部清理（2026-09-20 深夜，释放约 3.2 GB）：`D:\project\QingYangA、I`（重命名遗留的完整副本）、`D:\project\青阳AI`（空壳）、`D:\project\qingyang`、`D:\project\_apkbuild`（旧暂存）、`D:\project\_nugettest`；`D:\project\` 下现只剩 `QIngYangAI` 与两个无关 Unity 项目（`QiuHua`、`裂隙`，勿动）
- ⚠️ 工程暂无 `.git` 版本管理，清理后已无源码副本备份——建议尽快 `git init`；keystore 文件务必另行备份

---

## 五、打包命名规则（重要！）

**格式：`青阳AI-{大版本}.{月}.{日}-rc{当天序号}.apk`**

### 规则细节
| 项 | 规则 |
|----|------|
| 大版本 | 固定 **1** |
| 月/日 | **打包当天**的月份、日期（两位数前不补零，如 8月28日 → `1.8.28`）|
| rc 序号 | **每天从 1 开始**，同一天打第几个包就是几 |
| 文件名 | 必须和 versionName 一致 |

### 示例
- 昨天：`1.8.27-rc3` → 今天：`1.8.28-rc1`
- 今天再打一个：`1.8.28-rc2`、`1.8.28-rc3`...
- 明天：`1.8.29-rc1`

### csproj 里同步改两处
```xml
<ApplicationDisplayVersion>1.8.28-rc4</ApplicationDisplayVersion>  <!-- 对外版本名，带 rc -->
<ApplicationVersion>18</ApplicationVersion>                         <!-- 内部 versionCode，必须单调递增 -->
```
- `ApplicationDisplayVersion` = `1.8.{月}.{日}-rc{序号}`，每天重置
- `ApplicationVersion` = 每打一个包 **+1**（Android 要求递增，不能重置回小）

---

## 六、换模型（GLM 之类）怎么接

青阳AI 全部走 **OpenAI 兼容接口**，换模型 = 改设置：
1. 设置页 → 文本对话模型 → 填 GLM 的 API URL + Key + 模型名 → 保存
2. 多模态（视觉等）同理，各自填各自的 URL/Key/模型
3. 视觉识别请求体已对齐 SenseNova 官方格式（OpenAI 兼容），GLM 视觉一般也兼容

---

## 七、2026-08-28 陪伴能力升级（1.8.28-rc5 → rc6）

产品定位明确为**陪伴为主、帮你操作为辅**，本次新增：

### 7.1 四大基础能力（rc5）

| 功能 | 实现文件 | 要点 |
|------|----------|------|
| **SQLite 底座** | `ChatStore.cs` | 聊天/记忆/日记三张表（`qingyang.db3`）。旧 `chahis.qingyang` 首次启动自动迁移后改名 `.bak`。AI 生成图落盘 `images/` 目录，历史只存路径（修掉 base64 膨胀）。`ChatMsg` 新增 `Id/Timestamp/IsProactive` |
| **声音** | `TtsPlayer.cs` | OpenAI 兼容 `/audio/speech` → mp3 → MediaPlayer。回复完成自动朗读（可关）、AI 气泡「朗读」按钮重播、音色按配置套存。语音输入回退：系统识别失败且配置了听觉模型 → 录音 15s → `/audio/transcriptions` 转写 |
| **记忆+日记** | `MemoryService.cs` / `DiaryService.cs` / `DiaryPage` | 每 2 轮对话异步提取"关于用户的长期事实"（上限 100 条），注入 system 提示词；昨天聊得够多会自动写第一人称日记，聊天页标题栏「日记」入口可翻。设置页可清空记忆 |
| **主动关心** | `CareScheduler.cs` / `Platforms/Android/CareReceiver.cs` / `CareNotification.cs` | AlarmManager 每 15 分钟唤醒判定（非 WorkManager，无新包依赖）。场景：早安 7-11 点 / 深夜 23 点后在用 / 白天 4 小时冷场 / 36 小时失联。安静守则：免打扰时段、每日上限（默认 3）、两条间隔 ≥3 小时、总开关默认关闭。消息入库 + 系统通知，Android 13+ 动态申请通知权限 |

### 7.2 生活感知体系（rc6，免 Shizuku 优先）

**架构原则：能用系统 API 直接读的就不走 Shizuku；Shizuku 只负责"帮用户办事"的深度命令。**

- **感知中心 `DeviceContextService.cs`**：聚合所有数据源输出一段"用户此刻状态"文本，`ChatPage` 每次发消息前刷新并注入 system（`【你此刻能看到的用户状态】`），`CareReceiver` 主动消息同样复用。
  - 零权限项（永远有）：时间/星期、电量/充电、网络类型、铃声模式/是否在放媒体、屏幕亮灭、下一个闹钟
  - 授权项（总开关 `CareSenseEnabled` 门控，见下）：前台应用/使用时长、最近通知、今日日程
- **三项授权（设置页"主动关心"卡片内，含状态显示 + 一键跳转）**：
  1. **使用情况访问权**（`UsageStatsHelper.cs`，manifest `PACKAGE_USAGE_STATS`）→ 前台应用 + 今日 App 使用时长 Top3，**替代 dumpsys**；用户在"设置→特殊应用权限"里授权
  2. **通知使用权**（`CareNotificationListener.cs`，NotificationListenerService）→ 缓存最近 10 分钟的新消息通知（跳过自己/常驻/汇总），她能知道"微信来消息了"
  3. **日历**（`CalendarHelper.cs`，`READ_CALENDAR` 运行时弹窗授权）→ 今天剩余日程
  - Shizuku 仅作前台应用的兜底（UsageStats 未授权但 Shizuku 在线时）
- **人设重写（`AppSettings.BasePersona` / 六种性格）**：
  - 核心方向：**她先开口**——感知信息是"她主动问用户在干嘛"的由头，绝不等用户来查
  - 新增【生活感知】段：教她把感知信息用于"顺势关心"（熬夜劝睡、低电量提醒、日程提醒），**禁止复述数据/机器话/监控感**，用户表示不想被关注某方面要立刻尊重
  - 【终端命令能力】重新定位为"帮TA办事"（查私有DNS、存储、改设置），明确"了解状态不要跑命令"
  - 性格 prompt 中的 `dumpsys` 命令示例全部移除，改为基于自动感知的行为描述
  - 主动消息场景文案同步：冷场/失联场景要求"主动问问TA在忙什么"

### 7.3 流式稳定性修复（rc8，解决文字聊天 Socket closed / connection abort）

真机暴露的问题：发文字偶发 "Socket closed"；等回复时切后台，回来报 "Software caused connection abort"。根因是旧流式实现**边收边等**——每收 1 个字 sleep 20ms，一个长回复会把 HTTP 连接挂住几十秒，期间一切后台 Android 就冻结/掐断这条"看起来闲置"的连接。

修复（`ChatPage.xaml.cs`）：
1. **收流与显示分离**（`ConsumeSseAsync`）：网络侧全速收完立即关连接（掐线窗口从几十秒缩到几秒），收到的字进缓冲区；显示协程并发地按一秒五十字逐字上屏——**打字手感与旧版完全一致**。`{cmd}/{img}` 指令仍在网络侧实时检测，命中即断流进管线，行为不变。
2. **连接类异常自动重试一次**（`IsTransientNetworkError`：Socket closed / connection abort / Connection reset / Read error / 超时 / 网络不可达），重试前清空半截回复。
3. **报错文案人话化**（`FriendlyNetworkError`）：底层异常翻译成"网络连接被中断了（切后台太久被系统冻结、或服务端断开都可能）…"，不再把 "Socket closed" 原文甩用户脸上。
4. `TtsAutoPlay` 默认改为**关**（之前默认开，配置了 TTS 的用户每次回复都会多发一个语音请求，加重中转/服务端限流嫌疑）。

> 经验：商汤日日新等端点对"慢消费长连接"和超量请求会直接掐连接而不是返回规范错误，凡是流式请求都应快收快关；`Socket closed` / `Software caused connection abort` 一律按瞬时网络抖动处理（重试）。

### 7.4 两个真机 bug 修复（1.8.29-rc1）

1. **连续切换配置卡死**（`StatusBadge.cs`）：在线徽章的呼吸光晕是 `while(_running)` 无限动画循环，但标志位与在跑的动画之间有竞态——`StopPing` 只改标志不取消动画，动画跑完旧循环发现标志又被 `StartPing` 改回 true，循环只增不减；连点切配置时 5 个徽章各叠几十层动画循环把 UI 线程拖死。修复：改用**代际令牌**（`_pingEpoch`），`SetMode` 每次自增令牌，旧循环发现代际变化自行退出，并 `CancelAnimations()` 立即停掉在跑的动画——同一时刻每徽章至多一个循环。另给设置页全量连通检测加 500ms 防抖（`ScheduleFullRecheck`），连点切配置只跑最后一次检测。
2. **冷启动聊天页"从上滚到下"**（`ChatPage`）：旧流程是先绑定空集合再异步逐条加载，`KeepLastItemInView` 模式下每加一条就锚定最后一条，视觉上就是快速滚屏；且 `OnAppearing`（带滚动兜底）早于异步加载完成触发，存在竞态。修复：消息列表 XAML 初始 `IsVisible="False"`；历史加载完成后**一次性绑定 ItemsSource**，无动画定位到底部的多阶段兜底执行完（200ms 处）才把列表显示出来——冷启动直接呈现最后一屏。`_historyLoaded` 标志防 OnAppearing 与加载竞态（也顺带消除了 SyncNewFromDb 在加载完成前跑导致的重复加载隐患）。

### 7.5 自主思考：感知 API 工具箱 + 内心生活（1.8.29-rc2）

核心理念：从「app 喂数据给她」转为「**她想查就查、想说就说**」。隐藏指令协议扩展为 `{cmd}/{api}/{img}/{mood}/{avatar}/{say}`（解析统一在 `InstructionParser.cs`；聊天打字层会自动隐藏这些 token，只做副作用）。

| 模块 | 文件 | 说明 |
|------|------|------|
| **感知 API 工具箱** | `ApiRegistry.cs` + `DeviceContextService.cs` | 9 个命名 API：screen_time（屏幕使用时间）/foreground（前台应用）/notifications（最近通知）/calendar（今日日程）/battery/music/alarm/screen/network。AI 输出 `{api:"名"}` 主动调用，全部走系统 API 免 Shizuku（Shizuku 仅前台应用兜底），未授权返回说明文字。深度感知（使用时长/前台/通知/日程）**不再被动注入**，全靠她自己调用 |
| **API 调用气泡** | `ChatPage.RunApiPipeline` | 三行工具气泡标题动态化：显示"调用「屏幕使用时间」API"等（`ChatMsg.TerminalTitle` 持久化），`>_` 样式不变；结果回传模型做第二轮总结（`StreamSummaryReply` 已泛化为 toolResults 参数），并存 `TerminalExecLog` 作后续上下文 |
| **内心独白+心情** | `InnerLifeService.cs` | 每聊 2 轮触发 `ReflectAsync`：输出 JSON `{mood, thought}` 持久化（Preferences），心情表情显示在聊天标题栏，心里话注入 system（标注用户看不到）。回复末尾也可用 `{mood:"一个词"}` 自主更新 |
| **后台自主心跳** | `InnerLifeService.HeartbeatAsync` + `CareReceiver` | 15 分钟心跳先过硬闸门（`CareScheduler.CanSpeak`：总开关/免打扰/每日上限/3小时间隔），然后把局面交给模型：她自己决定调哪些 API（如早上 7 点查 screen_time 看睡得怎么样）、要不要开口、说什么（`{say:"..."}`）——规则只管"别烦人"，内容全靠自主 |
| **自绘头像** | `AvatarService.cs` | `{avatar:"自画像描述"}` 或设置页"让她画头像"按钮 → 文生图 → 存 `avatar.png` → 聊天标题栏圆形头像显示 |
| **记忆面板** | `MemoryListPage.xaml(.cs)` | 设置页"长期记忆 ›"进入：查看她记住的每一条、删单条（删除会同步刷新注入上下文） |
| **换模型泛化** | `AppSettings.ResolveChatModel` | 新增每配置套"思考模型"设置项（留空沿用 deepseek-reasoner 旧默认）；全项目模型选择统一走该函数（ChatPage 3 处/MemoryService/DiaryService），接 GLM 时在设置页填对应模型名即可。顺带修复 API Key 改动需重进设置页才生效的问题（`OnAppearing → ApplyApiKey`） |

### 7.6 小修：日记视角（1.8.29-rc3）

日记生成提示词有歧义（只说"以第一人称写"），模型会把"我"理解成用户、写出用户视角的日记。修复（`DiaryService.GenerateDiaryAsync`）：明确钉死角色——对话里标注「我」的每句都是她自己、「用户」是TA；日记的作者和「我」都是她；提到用户一律用「你/TA」；并明令禁止替用户写情绪（只能观察式地写「你看起来有点低落」）。已生成的旧日记不会自动改写，新日记生效。

### 7.7 设置页重排版（1.8.29-rc4，功能全保留）

按"模型 → 人设 → 对话行为 → 主动关心 → 记忆与形象 → Shizuku → 保存"分区重排，统一分区标题（emoji + 紫色小标）与卡片风格。**所有原有功能一个没删**：粘贴/清空按钮、强制思考、推理等级滑块、自动压缩开关+滑块、Shizuku 四行说明全部保留，仅调整位置与视觉层次。新增项（思考模型、音色/自动朗读、感知授权区、记忆与形象卡片）按所属分区归位。

### 7.8 推理等级移到聊天页（动态能力探测）+ 主界面重排（1.8.29-rc5）

- **推理等级动态化**：旧设置页滑块是假粒度（六档只有"是否走思考模型"两种结果），删除。改为：点「获取模型列表」时解析每个模型返回的推理档位（兼容 `reasoning_effort` 数组/字符串、`reasoning` true/对象、`supported_parameters`、`reasoning_levels` 多种厂商风格，`SettingsPage.ParseReasoningCapability`），保存模型选择时写入 `ReasoningSupported/ReasoningOptionsJson`。**聊天页顶部**仅在当前模型返回了档位时出现「思考:关/档位」按钮（档位数=模型返回数），点开选择，请求体自动附 `reasoning_effort`（`ChatPage.BuildChatBody`，聊天/总结/视觉三条链路统一）。换模型/切配置套时若档位不在新模型列表中自动重置为关。
- **强制思考开关保留在设置页**：作为模型未返回推理能力时的兜底（走"思考模型"设置项）。
- **主界面重排（聊天气泡未动）**：顶部栏"日记/设置"改纯图标腾位置、新增推理按钮列；输入框包圆角 Border、发送按钮圆角化；上下文/缓存字号微调。

### 7.9 模型配置气泡展开动画（1.8.29-rc6）

五个模型下拉气泡（文本/文生图/听觉/视觉/TTS）的展开/收起从"只有淡入淡出、内容瞬间出现"改为**高度生长动画**：展开时先按父容器宽度测量目标高度（`Measure`，加 2px 余量防裁切），从 0 生长到目标后恢复自动高度；收起时从当前高度缩到 0 再隐藏。全程 `QuadraticEaseOut`（二次方先快后慢），与人设卡片的高度动画同款手感（`SettingsPage.ToggleBubble`）。注意 MAUI 的 `Measure` 返回 `SizeRequest`，高度在 `Request.Height` 里。

### 7.10 称谓统一（1.8.29-rc7）

全项目所有指代 AI 的"她"统一替换为 **Ta**（16 个文件：界面文案、通知渠道名"Ta的消息"、弹窗、代码注释）。指代用户的 TA 保持不变。一处提示词特例：心跳情境里的"用户还没和她说过话"改为"用户还没**和你**说过话"——提示词中 Ta 专指用户，用"你"指代 AI 避免歧义。

### 7.11 玻璃画质 + 悬浮输入栏（1.8.29-rc8）

- **画质选择气泡**：设置页新增「🎨 界面画质」折叠气泡（同款二次方下拉动画），选项：关闭 / 毛玻璃 / 亚克力 / 液态玻璃，另有"标题栏下方渐变虚化"独立开关（`GlassEffect` + `TopBlurEnabled`，全局 Preferences）。
- **悬浮输入栏**：聊天页根布局改为单层 Grid 叠放——消息列表铺满全屏，输入栏 `VerticalOptions=End` 四边留白悬浮、无底边（`ChatPage.inputBar`），点空白处聚焦输入框。CollectionView 无 Padding 属性，底部避让用 Margin=92 实现。
- **三档效果映射**（`ChatPage.ApplyGlass`，切回聊天页时自动刷新）：毛玻璃=深色半透明磨砂；亚克力=偏冷紫蓝调更透；液态玻璃=最透+亮白高光描边。标题栏同样按档位半透明。
- **上糊下清渐变**：标题栏下 130px 双层渐变雾化层（深色雾 + 白色高光，`InputTransparent` 不挡点击），强度随档位变化；这是**仿真玻璃**（多层渐变+透明度），MAUI/Android 无廉价实时背景像素模糊，真·背景模糊需绑定 BlurView 类库，后续有需要再说。
- **rc9 关键修复**：聊天页根 Grid 的**子元素声明顺序就是 z 序**（后声明者在上）——初版把列表声明在标题栏之后，导致气泡浮在标题栏上、列表吞掉点击（设置/日记点不了）。正确顺序：**消息列表（最底）→ 雾化层 → 标题栏 → 悬浮输入栏（最顶）**。改悬浮布局时务必保持这个顺序。
- **rc10 补充**：悬浮输入栏背景与标题栏**完全同色**（同一档玻璃颜色，不再一深一浅）；系统底部导航栏（原紫色纯色）在 ApplyGlass 时通过 `Window.SetNavigationBarColor` 染成标题栏色并随档位联动（Android 15+ 边到边模式下该 API 被系统忽略，尽力而为）。另记录：本轮出现一次 IL1011 链接器写文件失败，同属文件占用类抖动，清洁重建后恢复——遇到 IL1011/XABAA7000/MSB6006 一律先重试再排查。
- **rc11 布局定稿（用户反馈修正）**：穿透+雾化方案被否——气泡不该钻进标题栏。最终结构：根 Grid 两行 `Auto,*`，标题栏在**第 0 行正常文档流**（不悬浮），列表占第 1 行（气泡到分界线即止，完全可见），悬浮输入栏 `Grid.Row=1 + VerticalOptions=End` 浮在列表上。雾化层已删除，`TopBlurEnabled` 设置项已移除；玻璃质感（三档配色+描边）保留在标题栏/输入栏/系统导航栏上。经验：MSB6006 连续两次时，删副本 obj/bin 清中间产物再构建即可恢复。

### 7.12 画质改为真·模糊滤镜（1.8.29-rc12）

用户实测发现染色方案无可见效果（rc11 后边框不再覆盖内容，透明色自然看不出来），并给出新规格：**滤镜直接作用于聊天内容，颜色恒定不加色**——毛玻璃=20% 模糊、亚克力=80% 模糊、液态玻璃=0%（纯透明占位）。

实现（`ChatPage.ApplyGlass`）：Android 12+ 的 `RenderEffect.CreateBlurEffect(radius, radius, Shader.TileMode.Clamp)` 直接挂到消息列表的 RecyclerView（`msgList.Handler.PlatformView`）渲染层，列表内容整体按档位实时模糊（毛玻璃≈5px、亚克力≈20px）；"关闭"与"液态玻璃"移除滤镜。Android 12 以下静默无效果。边框颜色恢复恒定（标题栏 #2A2A2A / 输入栏 #3C3C3C），不再按档位变色。

**绑定库坑**：本机 Mono.Android 绑定里 `CreateBlurEffect` 只有 `(float,float,Shader.TileMode)` 和 `(float,float,RenderEffect,Shader.TileMode)` 两个重载——没有无 TileMode 的两参数版，也没有 `BlurEdgeMode` 枚举，边缘行为用 `Shader.TileMode.Clamp`。

**rc13 推翻重做**：RenderEffect 方案依赖 Android 12+，用户实测设备（vivo/iQOO PD1921，Android 9~11）无效果。改为 **低分辨率采样滤镜**（`Platforms/Android/ContentFilterOverlay.cs`，BlurView 库同款算法）：每帧 OnPreDraw 时把列表画面缩到极小位图再放大回绘，采样倍率=模糊强度（毛玻璃 20%→缩放 0.35、亚克力 80%→缩放 0.05、液态玻璃 0%=隐藏滤镜），**全 Android 版本可用且是真模糊**。滤镜层为原生 View（触摸穿透、位置跟随列表），由 `ApplyGlass` 动态挂到列表的父容器。注意：Platforms/Android 代码里 MAUI 全局 using 会与 Android 类型冲突（View/Paint/IsVisible），一律用 `global::Android.*` 全限定名和 `ViewStates` 控制。

### 7.13 流程备忘（截至 rc12）

- 全项目指代 AI 的称谓为 **Ta**（指代用户的 TA 不变），提示词中 AI 用"你"、用户用"TA"
- 构建抖动三连（MSB6006 / XABAA7000 / IL1011）一律先重试；MSB6006 连续两次时删副本 obj/bin 清中间产物再构建
- 每次打包：直接在 `D:\project\QIngYangAI` publish（暂存目录已废弃）→ 复制回 `apk\` 与 `Z:\`（同名），流程见第四节

### 7.14 欢迎语与应用图标（rc14 / rc15）

- **欢迎页欢迎语**（rc14/rc16）：垂直位于打字行与顶部的正中间（top:24%），**水平靠左停靠**（left:20px，rc16 修正——rc14 曾误改居中），字号 16→20px（`IntroPage.BuildHtml` 的 `.welcome` CSS；想再调改 top/font-size 两个值）。
- **应用图标**（rc15）：更换为 `ligo.png`（蓝渐变 AI 对话气泡，1446×1443），已存为 `Resources\AppIcon\appicon.png`，csproj `MauiIcon Include` 直接指向该 PNG（`Color="#B0CDE4"` 兜底背景）。**坑**：MauiIcon 生成的 mipmap 资源名取自文件名——文件必须叫 `appicon.png`（资源名 `@mipmap/appicon`），否则 AndroidManifest 的 `android:icon="@mipmap/appicon"` 找不到资源报 APT2260。旧 SVG（appicon.svg/appiconfg.svg）留在目录未引用，回滚把 csproj 改回原行即可。

### 7.15 欢迎页厂商 Logo 滚动墙（1.8.29-rc17）

打字行与「立即体验」之间新增**三行厂商 Logo 无限滚动墙**：1/3 行向左、2 行向右，恒速 40px/s，CSS `translateX(-50%)` 无缝循环（内容复制到 ≥2 倍视口宽再整体翻倍，永不断档）。图标白底圆角芯片承托（深色底上彩色/黑色 Logo 都清晰），滚动与其他动画并行。

- **数据源**：`D:\project\QIngYangAI\svg\*.svg` → 构建时按**文件修改时间倒序**复制为 `Resources\Raw\ailogos\ai_001.svg…`（新厂商自动排最前），csproj 以 `MauiAsset` 打包。
- **运行时**：`IntroPage.GetLogoListAsync` 用 `AssetManager.List("ailogos")` 枚举（绑定只有单参数重载），`HtmlWebViewSource.BaseUrl = "file:///android_asset/"` 让相对路径 SVG 可加载；JS 按 i%3 分三行、失败芯片自动隐藏（不断档）。
- **新增厂商流程**：svg 丢进 `D:\project\QIngYangAI\svg` → 重新构建（或喊我"刷新 Logo 墙"）→ 自动排到所在行的最前面。
- 应用图标同时按最新 `ligo.png` 重新导入（rc17）。

### 7.16 三栏真玻璃（1.8.29-rc18，rc10 起失效的根因修复）

**用户指出的核心 bug**：rc10 把三根栏改成恒定实色 + rc11 标题栏回归文档流后，栏底下没有任何内容可透，玻璃效果"死了"。rc18 让玻璃真正长回三根栏上：

- **边到边**：`MainActivity.OnCreate` 设状态栏/导航栏透明 + `SetDecorFitsSystemWindows(false)`（API 30+）或 `LayoutNoLimits`（旧版）——内容延伸到两根系统条底下
- **三块玻璃垫层**：`ContentFilterOverlay`（低分辨率采样模糊，全版本可用）× 3，分别垫在标题栏/输入栏/系统导航栏（底部 navInset 高度条）下面，**按栏区域局部模糊**列表画面（源铺满父容器，坐标一致，源子区域=本层位置×采样率）
- **档位语义**：毛玻璃=20% 模糊、亚克力=80%、液态玻璃=0%（垫层隐藏，直接透出清晰内容）、关闭=栏实色+列表加避让边距（回到"不藏字"行为，关闭档专属）
- **状态栏避让**：标题栏 Padding 顶 += 状态栏高度；输入栏 Margin 底 += 导航栏高度
- **坑**：栏的镜像位置必须用原生视图（`xxx.Handler.PlatformView`），传 MAUI 控件会 CS1503；`DrawBitmap` 的源矩形是整数 `Rect` 不是 `RectF`；`AssetManager.List` 只有单参数重载

### 7.19 同意流程导航崩坏修复（1.8.30-rc7）

**闪退记录器首战告捷**：用户截图回传的崩溃为 `InvalidOperationException: PushAsync is not supported, please use a NavigationPage`（`ChatPage.OnSettingsClicked`）。根因：`AgreementPage` 同意后执行 `Windows[0].Page = new ChatPage()`——**裸页面替换窗口，没包 NavigationPage**，该会话内所有 PushAsync（设置/日记/记忆面板）全部失效；重启后走正常启动分支（有包裹）才恢复，所以表现为"只有同意后第一次打开设置闪退"。同根问题：欢迎页"立即体验"跳协议页也用 PushAsync，在裸窗口上会静默失败。

修复：同意后改为 `Windows[0].Page = new NavigationPage(new ChatPage())`；欢迎页跳协议页改为 `Windows[0].Page = new AgreementPage()`（窗口页面替换不依赖 NavigationPage）。**铁律：凡替换 `Windows[0].Page`，MAUI 页面必须包 `NavigationPage`（需要返回/推栈的）或直接用无需导航的页面。**

**rc8 追加铁律**：在 **WebView 的 Navigating 回调里**不能同步替换 `Windows[0].Page`——WebView 还在处理取消导航的收尾，回调栈里拆页面会原生层闪退（Java 崩溃，.NET 记录器抓不到）。必须 `e.Cancel = true` 后 `Dispatcher.DispatchDelayed(~80ms)` 延迟替换。

### 7.20 历史小节索引（rc5~rc16）

rc5=SQLite底座+声音+记忆日记+主动关心；rc6=感知API工具箱+内心生活；rc8=流式稳定性+报错人话化；rc9=徽章动画修复+冷启动直落底部；rc10=输入栏与标题栏同色+导航栏染色；rc11=标题栏回归文档流；rc12=画质改真·模糊滤镜；rc13=滤镜改低分辨率采样（全Android版本）；rc14=欢迎语位置字号调整；rc15=应用图标更换；rc16=欢迎语靠左修正；rc17=Logo滚动墙。

### 7.18 日期命名修正 + Logo 墙修复（1.8.30-rc2，2026-08-30）

- **日期命名**：跨零点后仍沿用 29 日版本号是失误——1.8.29-rc9~rc18 实际构建于 8/29 深夜至 8/30 凌晨。自本版起严格按"打包当天"命名：**1.8.30-rc2**（8/30 零点后的 rc18 等同当天 rc1，本版顺延 rc2）。以后每天第一包都是 `1.8.{月}.{日}-rc1`。
- **Logo 墙不显示的根因**：MauiAsset 打包后 Assets 实际路径带全前缀 `assets/Resources/Raw/ailogos/`，代码却按 `ailogos/` 枚举和加载——全部 404。修正为 `List("Resources/Raw/ailogos")` + 相对路径 `Resources/Raw/ailogos/x.svg`（配合 BaseUrl `file:///android_asset/`）。**坑**：MauiAsset 的运行时路径保留 `Resources/Raw` 前缀，别按模板文档想当然地剥掉。

### 7.19 玻璃垫层挂载容器修正 + 滤镜诊断（1.8.30-rc11）

用户反馈"档位切了仍无模糊"。两个动作：
1. **垫层挂载容器改到栏所在的根网格容器**（原挂在列表自己的父容器——若 MAUI 为 CollectionView 包了中间容器，垫层会被裁剪或坐标错位）；插入位置 = 标题栏之前（列表之上、栏之下）。
2. **滤镜诊断**：ApplyGlass 把垫层实际状态（父容器类名/挂载索引/三层可见性与尺寸）写入 `GlassDiag`（Preferences），设置页画质气泡内新增「滤镜诊断」按钮，一键弹窗展示。模糊仍不生效时，把诊断内容发来即可精确定位卡在哪一层。

**注意点（后续开发必读）**：
- 强制思考时模型名仍硬编码 `deepseek-reasoner`（`ChatPage.xaml.cs`），接 GLM 前要泛化
- SQLite 包 `sqlite-net-pcl 1.9.172` + `SQLitePCLRaw.bundle_green 2.1.8`；XA0141 警告（e_sqlite3 非 16KB 页对齐）在 Android 16 才会强制，届时升级 SQLitePCLRaw 即可
- 主动关心感知前台应用优先走使用情况访问权（免 Shizuku），Shizuku 仅兜底；通知感知需通知使用权，日历需 READ_CALENDAR
- 后台主动消息靠 `ChatPage.OnAppearing → SyncNewFromDbAsync` 增量同步进界面

### 7.20 玻璃底色近透明 + PixelCopy 生命周期 + Logo 墙修复（1.8.31-rc2）

**玻璃发黑失效的深层机制**：PixelCopy 截取的是整窗渲染结果，**包含栏自己的半透明底色**——垫层把已含底色的画面画回去、栏再叠一层，每 150ms 复合一次，一两秒内收敛成实色深底，玻璃感消失。修复：玻璃档栏底色改为**近全透明**（#14222222），模糊层承担全部玻璃观感；描边按档位区分（毛玻璃/亚克力/液态玻璃高光递增），符合"颜色恒定无色，事实上一层滤镜"规格。同时补回多轮改写中丢失的输入栏描边赋值。

**PixelCopy 耗电**：原循环每 150ms 截屏、页面销毁/切后台不停。修复：`OnDetachedFromWindow` 停止、`OnAttachedToWindow` 恢复（垫层随窗口生命周期）。

**Logo 墙快慢不一 / 中间行文心到边消失**：根因是旧逻辑用 `guard` 上限 + `translateX(-50%)`，但 `guard++ < 12` 在旧 WebView 下 `scrollWidth` 未即时重算，可能不落在"整组"断点 → 平移回起点时左右两半对不齐 → 闪跳像"快"、且中间行滚到边对齐失败像"消失"。修复：**只按整组翻倍**（先按整组复制到 ≥2 倍视口宽，再 `half+half` 精确翻倍），保证 50% 处左右半份严格相等；速度恒为 (单份宽)/40 px/s，覆盖安装与清数据一致。

### 7.21 全面取消玻璃（1.8.31-rc5）

**决策背景**：毛玻璃/亚克力在用户设备（vivo/OriginOS，Android 10）上反复无法实现真模糊——三套方案（source.Draw 采样、PixelCopy 垫层、PixelCopy 设栏背景）全部"完全透明"。判断根因为 OriginOS 对窗口 surface 的管控使 PixelCopy/Draw 无法捕获内容，且 Android 10 无 RenderEffect。经用户确认：**全面取消玻璃效果**，不再追求真模糊。

**变更**：
- `ChatPage.ApplyGlass` 改为纯布局整理：标题栏实色 `#2A2A2A`；**悬浮输入栏完全透明**（无纯色背板、无描边，XAML 与运行时双保险）；**聊天列表顶部让出标题栏、底部让出发送栏高度+16**（气泡最低位在发送栏上方）。
- `BarGlass.cs`、`ContentFilterOverlay.cs` 玻璃实现代码移除（不再调用）。
- 设置页「🎨 界面画质/玻璃效果」区块整体移除（含玻璃样式选择、滤镜诊断按钮及其 C# 处理器、ParseUi/Save 引用）。
- `AppSettings.GlassEffect`/`TopBlurEnabled` 属性保留但不再被引用（无害，留待未来）。

### 7.26 Super Admin 深度权限（1.9.7-rc5，⚠️ 高风险）

用户要求给 AI "super admin" 能力，设置页新增独立分区「🦾 Super Admin（⚠️ 高风险）」。

- **两个深度权限 + 独立开关 + 风险红字说明**：
  1. **存储访问权限**（`MANAGE_EXTERNAL_STORAGE`，全盘读写文件）：`SuperAdmin.OpenStorageSettings()` 跳系统"所有文件访问"设置；`IsStorageGranted()` 检查。
  2. **设备管理器**（`DevicePolicyManager`）：`QyAdminReceiver`（DeviceAdminReceiver）经 `RequestAdmin()` 拉起系统激活界面；`IsAdminActive()`/`RemoveAdmin()`。
- 资源：`Platforms/Android/Resources/xml/qy_device_admin.xml`（uses-policies 全开）、`values/strings.xml`（qy_admin_desc）。
- 风险提示明示在 UI：可能让手机无法轻易卸载、AI 可读写全盘/锁屏/清数据——**仅在用户完全信任时开启**。Android 特殊权限均需用户到系统设置手动开启，本类提供一键跳转。
- **注意**：设备管理器权限目前只做了"激活/状态/开关"，**尚未接入任何具体 AI 调用动作**——后续若让模型用它（锁屏/清数据等）需逐条加护栏确认。收尾处理（Recovery 页）优先顺延。

用户反馈"必须点破查什么她才查，从不主动了解"。新增 `CareWatch.cs`：**内部关注清单（不展示给用户）+ 定期默默观察**。

- **关注清单**（代码内置，后台轮转，每 45 分钟最多一次）：早上 6-11 查 screen_time（睡得好不好）；白天 9-22 查 foreground（在忙什么）；午后查 screen_time（今天用手机久不久）；深夜 22-5 查 screen（睡没睡）；随时查 notifications（有没有新消息）。
- **触发**：`CareReceiver.HeartbeatAsync` **先无条件 `CareWatch.ObserveOnceAsync()`**（只要"生活感知"开关开着就默默查，与是否开口解耦——原来 CanSpeak 硬闸门 return 会挡掉观察，是缺陷，已修）；结果存 Preferences（CareWatchLastAt/Text/Reason），**不发通知不展示**。
- **注入**：观察到的近况（≤8 小时内）通过 `CareWatch.BuildObservedContext()` 注入——当面聊天（ChatPage `_observedContext`）、快捷回复（ReplyToUserAsync）、主动心跳（Heartbeat situation）三处，提示词注明"你刚才悄悄留意到的…用户看不到，当自己亲眼看到的自然带进对话，别复述数据"。
- **依赖**：需"生活感知"开关开 + 对应 API 授权，否则观察结果为空不记录（避免注入废话）。
- **效果**：她会在后台自己"记挂"你——早上偷偷看睡眠、深夜看你睡没睡、白天看你忙什么，下次聊天自然带出"你昨晚是不是又熬夜了"，无需你点破。新增关注项只需在 Watchlist 数组加一条。

- **输入栏按钮还原圆形**：语音/图片两按钮恢复为 40×40 深色圆钮（#444444、RoundRectangle 20），圆外全透明，**显式 StrokeThickness=0** 杀掉 MAUI Border 默认描边（用户看到的"框线"就是透明 Border 的默认描边在部分版本上被绘制）。四控件仍整体括在圆角 20 的大气泡内。
- **气泡美化**：AI 气泡 #444444→#3A3A3C、用户气泡保持紫色，两者圆角 14→18，消息间距 8,6→10,8。
- **版本命名提醒**：跨零点构建时务必按当天日期重算版本号（1.8.31-rc4~rc14 一批实际构建于 9/2 深夜至 9/3，已按 1.9.3-rc1 重新起算修正）。
- **真模糊的经验教训**：Android 10（尤其 OriginOS/MIUI 等深度定制）上，实时背景模糊无可靠路径——窗口捕获（PixelCopy）与软件绘制（View.Draw）都可能被 OEM 掐掉；要真模糊需第三方 BlurView 库（其底层同样依赖 RenderScript/捕获，此机型仍不保证）。玻璃实现相关代码（BarGlass/ContentFilterOverlay）已从项目移除，如需恢复参考交接文档 git 历史或 7.16~7.20 小节。

### 7.21 三栏真玻璃（1.8.29-rc18，rc10 起失效的根因修复）

**用户指出的核心 bug**：rc10 把三根栏改成恒定实色 + rc11 标题栏回归文档流后，栏底下没有任何内容可透，玻璃效果"死了"。rc18 让玻璃真正长回三根栏上：

- **边到边**：`MainActivity.OnCreate` 设状态栏/导航栏透明 + `SetDecorFitsSystemWindows(false)`（API 30+）或 `LayoutNoLimits`（旧版）——内容延伸到两根系统条底下
- **三块玻璃垫层**：`ContentFilterOverlay`（低分辨率采样模糊，全版本可用）× 3，分别垫在标题栏/输入栏/系统导航栏（底部 navInset 高度条）下面，**按栏区域局部模糊**列表画面
- **档位语义**：毛玻璃=20% 模糊、亚克力=80%、液态玻璃=0%（垫层隐藏，直接透出清晰内容）、关闭=栏实色+列表加避让边距（回到"不藏字"行为，关闭档专属）
- **状态栏避让**：标题栏 Padding 顶 += 状态栏高度；输入栏 Margin 底 += 导航栏高度
- **坑**：栏的镜像位置必须用原生视图（`xxx.Handler.PlatformView`），传 MAUI 控件会 CS1503；`DrawBitmap` 的源矩形是整数 `Rect` 不是 `RectF`；`AssetManager.List` 只有单参数重载；AppWidget 绑定命名空间是 `Android.Appwidget`
- **rc3 补充（标题透明+让位并存）**：玻璃档列表顶部边距为 0（气泡从标题栏下穿过，被垫层模糊出轮廓，标题栏才真正透明）；关闭档顶部边距=标题栏实测高度（气泡到线即止）
- **rc10 补充**：曾按用户要求改成"所有档位气泡都让出标题栏"——导致标题栏下无内容可透、玻璃又失效，已还原：**玻璃档穿栏（透明可见）、关闭档避让**为最终行为；标题栏始终 `VerticalOptions=Start` 吸附顶端

### 7.22 陪伴四件套（1.8.30-rc9）

- **相伴天数**：`ChatStore.EnsureCompanionSinceAsync` 首次初始化记下最早消息时间（`CompanionSince` 只写一次）；设置页"记忆与形象"与日记页顶部显示"💗 已陪伴 N 天"
- **备份/恢复**：`BackupService.ExportAsync` 打包全部消息/记忆/日记/首次相遇/头像为单个 JSON（设置页导出 → 分享面板）；导入为**覆盖式**恢复（FilePicker → `RestoreAllAsync`）。聊天图片为本地路径，恢复后旧图不可迁移
- **通知栏快捷回复**：通知带 `RemoteInput` 回复框 → `DirectReplyReceiver` → 用户消息入库 → `InnerLifeService.ReplyToUserAsync`（人设+感知+记忆+日记+内心+最近 8 条）生成 ≤80 字回应 → 回复入库 → 刷新通知（可连续多轮）；打开 app 由 `SyncNewFromDbAsync` 补显对话
- **桌面小组件**：`CareWidgetProvider`（`@xml/widget_care_info` + `@layout/widget_care`）显示最新一句话+心情+相伴天数，点击进聊天页；`CareWidget.RefreshAll` 在启动/主动消息/快捷回复后刷新。坑：AppWidget 绑定命名空间 `Android.Appwidget`；widget 资源放 `Platforms/Android/Resources/{layout,drawable,xml}` 自动打包；provider 需 `Exported=true`

### 7.27 鸿蒙通知感知 + 无障碍通道（1.9.7-rc7，versionCode 75）

用户设备：**华为鸿蒙 OS 4.0**。鸿蒙上 `NotificationListenerService` 不可靠（经常"服务尚未连接"），Shizuku 拔 USB 即失效，无无线调试。

**四路智能切换通知感知**（`DeviceContextService.GetNotificationsTextAsync`）：
1. ① NotificationListener（`CareNotificationListener.IsEnabled()`）—— Android 标准通道
2. ② 无障碍服务（`QyAccessibilityService.IsEnabled()`）—— **鸿蒙可用，无 USB 依赖**
3. ③ Shizuku `dumpsys notification --noredact`（Shizuku 在线时兜底）
4. ④ fallback 文案

**新增文件**：
- `Platforms/Android/QyAccessibilityService.cs`：`AccessibilityService`，监听 `NotificationStateChanged`，写入 `CareNotificationListener.Store()`（共享存储）。`IsEnabled()` 查 `Settings.Secure.EnabledAccessibilityServices`；`OpenSettings()` 跳无障碍设置页
- `Platforms/Android/Resources/xml/qy_accessibility.xml`：无障碍服务配置
- `Platforms/Android/Resources/values/strings.xml`：qy_accessibility_desc

**改动**：
- `CareNotificationListener.Store()` 改为 `public`（无障碍服务共享）；持久化存储改用 Preferences（JSON 数组，重启/服务回收不丢）；`OnListenerConnected` 记录连接时间戳
- `AndroidManifest.xml`：声明 `QyAccessibilityService`（`BIND_ACCESSIBILITY_SERVICE`）
- 设置页"主动关心"卡片新增无障碍授权行（状态显示 + 一键跳转）

### 7.28 内置浏览器（AI 上网插件）+ 每条消息 AI 必看 + 主题色（1.9.8~1.9.11）

**三批新功能**：

#### A. AI 上网浏览工具（`BrowserTool.cs`）
- **不是给用户的浏览器 UI，是 AI 自己用的后台工具**
- AI 输出 `{browse:"url或搜索词"}` → 客户端后台用 HttpClient（走宿主网络栈：Wi-Fi/蜂窝/DNS/代理/CA）抓取网页 → 提取正文（去 script/style/nav/footer/header，取 main > article > body，去标签，压缩空白，截断 6000 字）→ 回传给 AI → AI 用亲密语气自然接话
- 一次最多 3 条 `{browse:...}`，支持并发
- BasePersona 新增【上网浏览能力】说明段
- 接入管线：`ConsumeSseAsync` 检测 `{browse:"..."}` → `CancelledForBrowse` 断流 → `RunBrowsePipeline` → `BrowserTool.FetchAsync` → `StreamSummaryReply` 结果回传
- `SseConsumeResult` 新增 `CancelledForBrowse` 标志

> **设计变更记录**：最初做成了独立浏览器页面（`BrowserPage.xaml`，带地址栏/前进后退/分享/问 AI 等 UI），用户反馈"不是让你在应用里打开浏览器，而是让 AI 可以访问这个浏览器"——重做为纯后台插件，删除全部 UI，只保留 `BrowserTool.cs`。

#### B. 每条消息 AI 必看（`MessageReviewService.cs`）
- **独立开关**（`AppSettings.ReviewEachMessage`，默认关闭），独立于「主动关心」和「生活感知」
- 用户每次发送消息后，AI 用当前模型私下复盘 4 维度：**情绪 / 意图 / 潜台词 / 关系走向**
- 严格格式化输出（4 行），最多 120 字，只进下一轮 system prompt，**不上屏、不写库、不进历史**
- 最多保留 6 条复盘缓存，不膨胀 prompt
- 一键清空缓存按钮（不关开关）
- 清聊天数据时自动一起清空
- 复盘失败静默降级，不影响正常对话
- 注入：`ChatPage.BuildHistoryMessages` 的 system 里追加 `_reviewContext`

#### C. 主题色自定义（`AppSettings.ThemeColorHex`）
- 设置页顶部新增「🎨 主题色」卡片，8 个预设色圆点：**紫色（默认 #B388FF）/ 粉色 / 蓝色 / 绿色 / 橙色 / 红色 / 青色 / 金色**
- 选中后即时生效（不用点保存），白圈描边标记当前选中
- 递归遍历可视树（`ApplyThemeToVisualTree`），把所有 `#FBB5B2`（旧粉色）的 Label/Button/Border/MorphIcon 换成选中色
- ChatPage 和 SettingsPage 的 `OnAppearing` 自动调用 `ApplyThemeColor()`

### 7.29 主动关心不生效修复 + 简短回复纪律（1.9.10-rc1，versionCode 78）

**Bug 1：主动关心从不触发**
- **根因**：App 启动时**从未调用 `CareReceiver.EnsureScheduled()`**——AlarmManager 心跳从没排过期。只有用户在设置页手动开关"主动关心"时才会排期，杀进程重启后排期丢失。
- **修复**：`App.xaml.cs` 构造函数加 `ScheduleCareIfEnabled()`，启动时如果 `CareEnabled == true` 就立即排期。
- **同时修了 `CareBootReceiver`**：`Exported = false` → `true`（BOOT_COMPLETED 是系统广播，Exported=false 收不到）；加 `AppSettings.CareEnabled` 检查。

**Bug 2：AI 主动调用 API 不生效**
- **根因同上**：心跳里的 API 调用链路本身是通的（`HeartbeatAsync` → 模型输出 `{api:"名"}` → `ApiRegistry.ExecuteAsync` → 结果回传 → `{say:"..."}`），但心跳**从未被触发过**（Bug 1），所以 API 调用自然不生效。修了 Bug 1 后链路恢复。

**Bug 3：回复太长**
- BasePersona【输出纪律】新增第 4 条：**必须简短回复：日常对话回复控制在 1-3 句话以内（一般不超过 50 字），除非用户明确要求详细解释或长文。像真人发微信一样短、轻、自然，不要写作文。**

### 7.30 MorphIcon 新增 9 个图标

为支持浏览器 UI（后来删除但图标保留，供未来使用）：
`Globe` / `Wifi` / `ArrowLeft` / `ArrowRight` / `RotateCw` / `MessageCircle` / `MoreVertical` / `Home` / `Search`

### 7.31 MAUI 10 WebView API 踩坑记录

在开发 BrowserPage 时发现 MAUI 10 的 WebView API 与文档/旧版不一致：
- `WebNavigatedEventArgs.Uri` → 实际叫 `Url`（继承自 `WebNavigationEventArgs`）
- `WebViewSource` 是基类，`WebSource` 不存在；设源用 `new UrlWebViewSource { Url = url }`
- `LoadingProgress` / `NavigatedToHistoryEntry` / `PageDown` 事件/方法**不存在**
- `WebSettings.CacheModeDefault` 不存在
- `LineBreakMode.TailTruncate` 不存在（用 `WordWrap`）
- `ProgressBar.IsIndeterminate` 不存在
- `ActivityFlags` 不在 `Android.App` 命名空间下
- `Descendants<T>()` 扩展方法不存在，需手写递归遍历 `Layout.Children`（类型是 `IView`，需 `is VisualElement` 转换）
- `Border.Stroke` 是 `Brush` 类型不是 `Color`，比较要用 `SolidColorBrush` 取 `.Color`

### 7.32 新用户快速开始引导（1.9.12-rc1）

**入口变化**：勾选隐私政策后**不再直接进聊天页**，先进 `QuickStartPage`，走完（或跳过）才进 `ChatPage`。

| 项 | 说明 |
|----|------|
| 文件 | `QuickStartPage.xaml(.cs)`（新增） |
| 触发 | `AgreementPage.OnAgreeClicked` → `new NavigationPage(new QuickStartPage())` |
| 完成标记 | `Preferences["QuickStartDone"]=true`；`App.CreateWindow` 见未标记则再进引导 |
| 老用户兼容 | `App.MarkOldUserQuickStartDone()`：升级前已同意协议且已配好 API Key 的，自动标记完成，不强制重走 |

**结构**：单页两步（底部按钮区常驻，不随步骤切换）
- 根 Grid `Auto,*,Auto`：顶部标题+步骤条 → 中部两个 ScrollView 叠放（`stepModel` / `stepPersona`）→ 底部按钮行
- **左下角「跳过」常驻**：保存已填内容后直接进聊天页，不做任何校验
- 右侧「上一步 / 下一步」，第二步按钮变「开始聊天」；Android 返回键在第二步退回第一步

**第 1 页（模型）**：
- 文本对话模型**独立气泡，默认展开**（引导主角，不能一上来就收着）
- 「更多模型」气泡**默认关闭**，里面括 4 个子气泡（文生图/听觉/视觉/TTS），子气泡各自可折叠，默认全关
- 交互与设置页完全一致：粘贴/清空、获取模型列表、Picker、思考模型

**第 2 页（人设）**：**排版与设置页「💬 Ta的人设」一模一样**——官方预设 / 自定义两个小气泡 + 内容盒（预设=性格 Picker，自定义=140 高 Editor）

**动画（与设置页同款）**：
- 气泡：`ToggleBubble` 高度 0→测量值（260ms）+ 淡入（220ms），收起反向（200/180ms），全程 `QuadraticEaseOut = t => 1-(1-t)*(1-t)`
- 步骤切换：旧页淡出+横移 24px（180ms），新页从反向淡入归位（260ms），同为二次方 ease-out
- 人设内容盒高度 72/176 过渡 + 标题白→灰，同设置页

**注意**：
- 保存逻辑独立实现（`SaveModelConfigFromUi` / `SavePersonaFromUi`），**人设保存时官方预设 = Persona 留空**（设置页那边是无论哪个模式都写编辑器内容，属历史行为，未改）
- 主按钮 `btnNext` 取 `AppSettings.ThemeColor` 跟随主题色
- 引导页 `NavigationPage.SetHasNavigationBar(this,false)`，不显示导航栏
- 结束一律 `Windows[0].Page = new NavigationPage(new ChatPage())`（铁律：必须包 NavigationPage）

### 7.32 新用户快速开始引导（1.9.12）

新增 `QuickStartPage.xaml(.cs)`：面向新用户的两步引导，插在「同意协议」与「聊天页」之间。

- **入口改动**
  - `AgreementPage.OnAgreeClicked`：同意后改为 `Windows[0].Page = new NavigationPage(new QuickStartPage())`
  - `App.CreateWindow`：已同意但 `Preferences["QuickStartDone"] == false` → 进引导；否则直接进聊天页
  - **老用户兼容**：`App.MarkOldUserQuickStartDone()` —— 升级前已同意过协议且 ApiKey 非空的用户直接标记完成，不被强制走引导；没配好模型的仍会进引导（正好帮他接上）。完成标记写在 `Preferences["QuickStartDone"]`
- **页面结构**：外层 Grid 三行 = 顶部（标题 + 步骤文案 + 两段进度条）/ 中部（两个 ScrollView 叠放，只显示一个）/ 底部栏
  - **左下角「跳过」常驻**：整个引导过程中位置不变，点了会保存已填内容再进聊天页，不强制任何一项
  - 底部右侧：第 2 步才出现的「上一步」+「下一步 / 开始聊天」，主按钮颜色跟随 `AppSettings.ThemeColor`
- **第 1 页（模型配置）**
  - 文本对话模型**独立一个气泡，默认展开**（API Key / API URL / 获取模型列表 / 模型选择 / 思考模型）
  - 「更多模型」气泡**默认收起**，里面是 文生图 / 听觉 / 视觉 / TTS 四个可折叠子气泡（各自带 chevron，二次折叠）
  - 五组配置的保存走 `SaveModelConfigFromUi()`，与设置页一致（写入当前激活的配置套）
- **第 2 页（人设配置）**：排版与设置页「💬 Ta的人设」**完全一致**——标题 + 副标题 / 官方预设·自定义 两个小气泡 / 内容盒（预设=性格 Picker，自定义=Editor）。保存走 `SavePersonaFromUi()`，**官方预设 = Persona 留空**（修掉设置页"切回预设仍写入旧自定义文本"的隐患）
- **动画**：气泡展开/收起沿用设置页同款 `ToggleBubble`（高度 0 ↔ 测量目标高度 + 淡入淡出，`QuadraticEaseOut` 先快后慢）；步骤切换为「旧页淡出 + 横移 24px，新页从反方向淡入归位」，同样二次方曲线
- **坑**
  - Android 返回键需 `OnBackButtonPressed` 拦截：第 2 步退回第 1 步，而不是直接退出
  - `Finish()` 里替换窗口页面必须 `new NavigationPage(new ChatPage())`（裸页面会让 PushAsync 全失效）
  - 引导页 `NavigationPage.SetHasNavigationBar(this, false)` 隐藏导航栏

### 7.33 打包卡在 XARMM7024（2026-09-12，已解决）

现象：`dotnet publish -f net10.0-android -c Release` 在 `RewriteMarshalMethods` 阶段报
`error XARMM7024: System.IO.IOException: 当文件已存在时，无法创建该文件`。此时 `QingYangAI.dll` 已编译成功，卡的是后续 JNI marshal methods 重写。

**最终解法**（18:23 那次打包验证有效）：**清掉副本的 `bin`/`obj` 后原样重试同一条命令**——
`cmd /c "rmdir /S /Q D:\project\qingyang\bin & rmdir /S /Q D:\project\qingyang\obj"` 然后重新 xcopy + publish，一次通过。
这个错是抖动类的，失败了别怀疑代码，清了中间产物再来一遍就好。

**排查记录**（当时以为改坏了代码，其实没有）：删 QuickStartPage 回退入口后重打照样报错 → 与业务代码无关；期间试过的 build-server shutdown、-nodeReuse:false、单 RID 都没绕过，最后还是"清干净+重试"最管用。

### 7.34 标题栏常驻 token 统计（1.9.12）

需求：在 `NavigationPage.TitleView` 常驻显示两类统计，且**兼容所有大模型厂商的 usage 字段命名**。

新增文件 `TokenUsageStats.cs`，三个部分：

1. **`TokenUsage`（内部模型）**：只有 `PromptTokens` / `CachedTokens` / `HitRate` 三个成员，厂商差异全部在映射层消化掉。
2. **`TokenUsageMapper`（字段映射层）**：`FromJson(string)` / `FromElement(JsonElement?)`
   - prompt 别名：`prompt_tokens` / `input_tokens` / `prompt_token_count` / `input_token_count` / `inputTokens` / `promptTokens` …
   - cached 别名：`cached_tokens` / `cache_tokens` / `prompt_cache_hit_tokens` / `cache_read_input_tokens` / `cachedContentTokenCount` …
   - 嵌套容器：`prompt_tokens_details` / `prompt_cache_hit_token_details` / `input_tokens_details` …（顶层找不到就去里面找一轮）
   - 匹配规则：先精确匹配，再按「忽略大小写 + 忽略 `_`/`-`/空格」匹配（所以 `promptTokens` 能匹配 `prompt_tokens`）
   - 特殊处理：Anthropic 风格 `input_tokens` 不含 `cache_read_input_tokens`，命中时自动加回；DeepSeek 风格只有 命中/未命中 时反推 `prompt = hit + miss`；`cached > prompt` 自动收敛
   - **任何情况下都不抛异常**，缺字段返回 0（无缓存字段 → cached = 0）
3. **`UsageStats : INotifyPropertyChanged`（单例 `UsageStats.Current`）**
   - 本次：`LastPromptTokens` / `LastCachedTokens` / `LastHitRate` / `LastText`
   - 全局：`TotalPromptTokens` / `TotalCachedTokens` / `TotalHitRate` / `TotalText`
   - `Apply(TokenUsage)`：更新本次 + 累加全局，自动 `OnPropertyChanged` 触发 UI 刷新；内部走 `MainThread.BeginInvokeOnMainThread`，后台线程调用也安全
   - 累计数持久化在 `Preferences`（`UsageStats.TotalPromptTokens` / `UsageStats.TotalCachedTokens`），重启不清零；`ResetTotals()` 可重置

**接入点（ChatPage.xaml.cs）**
- `StreamChunk.usage` 类型从 `UsageItem?` 改成 `JsonElement?` —— 保留原始 JSON，才能做通用映射（原来的 `UsageItem`/`CacheUsage` 只认 DeepSeek 一套字段，已停用但保留）
- 主对话 SSE 循环：`TokenUsageMapper.FromElement` → `UsageStats.Current.Apply`
- 非流式摘要调用：`UsageStats.Current.ApplyFromJson(respJson)`
- `NavigationPage.SetHasNavigationBar(this, true)`（原来 ChatPage 是隐藏导航栏的，现在为了常驻统计打开）

**UI（ChatPage.xaml）**：`<NavigationPage.TitleView>` 里 `HorizontalStackLayout`——左侧「青阳AI」，右侧两行浅灰小字（`LastText` / `TotalText`，10 号字，`#9E8D8C` / `#8A7B7A`），绑定源用 `{x:Static local:UsageStats.Current}`。

**注意**：ChatPage 自带一个 `titleBar`（头像/心情/思考开关/日记/设置），打开系统导航栏后会变成上下两条栏。如果嫌挤，可以把 `titleBar` 里的 `lblTitle` 去掉只留功能图标。

---

## 八、9/16 主界面修复与统计栏重建（1.9.16-rc1 ~ rc4）

> 交接日期：2026-09-16。本轮集中修复主界面（标题栏双栏/消息列表/传输统计），版本线从 1.9.13 跨到 1.9.16（09-14 中途跨天，按打包当天日期起名）。

### 8.1 标题栏整合（去掉双栏）
- **原状**：系统导航栏（`SetHasNavigationBar(true)`）+ 自定义 titleBar 两条栏；下方还有一条 menuBar 放「访问权限/模型设置/思考强度」三个占位按钮和日记/设置图标。
- **改后**：
  - `NavigationPage.SetHasNavigationBar(this, false)`，关掉系统导航栏，只留自定义 titleBar。
  - 删除 menuBar，三个占位按钮（访问权限/模型设置/思考强度）连同其事件处理器一并移除。
  - titleBar 单行整合：左 = 头像 + 标题 + 心情；右 = 推理芯片 + 上下文 + 缓存命中 + 日记 + 设置。
- **注意**：ChatPage 的 `ApplyThemeToVisualTree` 对 `StyleId=="chatBubble"`（用户气泡）与 `btnSend` 跳过主题色染色，避免从设置页返回后气泡/发送按钮被染成主题色（曾出现"变蓝"）。

### 8.2 消息列表显示修复（曾多次"不显示/灰屏/在最顶"）
根因链与最终修法：
1. CollectionView 必须绑定 `ItemsSource="{Binding Messages}"`，**BindingContext = this 要在 Messages 初始化之后设**（否则绑定拿到 null 永久失效）。
2. XAML 初始 `IsVisible="False"`（历史加载完再显示）；`InitAsync` 里**把 `msgList.IsVisible = true` 提前到 try 之前**，确保任何异常都不会让整页只剩标题栏（曾出现灰屏）。
3. 去掉 `ItemsUpdatingScrollMode="KeepLastItemInView"`（它会在逐条加载时带动画滚动，看起来"从上面滚下来"）。
4. 定位底部：加载后 `Dispatcher.DispatchDelayed(250ms)` 一次 `ScrollTo(List.Count-1, End, animate:false)` 即可，不要用 for 循环反复滚（会造成反复滚动感）。
5. `OnAppearing` 整体 try-catch；`_historyLoaded` 判断防 InitAsync 与 OnAppearing 双负载（若有双负载会消息重复、滚动混乱）。

### 8.3 云端 token 统计栏（上下文 / 缓存命中）
**用户要求格式**：`上下文:352K/1M·35.2%`（占用/模型窗口·占比）、`缓存命中:98%`（只显示百分比），数据从模型返回的 usage 抓取。
- 实现：`UpdateContextLabel` 读 `UsageStats.Current.LastPromptTokens` 与 `AppSettings.EffectiveMaxTokens`（模型窗口，来自 `/models` 返回的 `context_length` 等字段）；`UpdateCacheHitLabel` 读 `UsageStats.Current.LastHitRate`。
- 两个标签同色 `#9E8D8C`（避免被主题色染色）。
- **重要坑①**：DeepSeek 等模型**流式响应默认不带 usage**，请求体必须加 `stream_options: { include_usage: true }`（`BuildChatBody` 已加，仅 stream=true 时带），否则统计栏一直是 0。
- **重要坑②**：usage 字段别名已加 `total_tokens` 兜底（部分中转只回 total 不回 prompt）。
- **重要坑③**：若模型/中转完全不回 usage（有些中转摘掉省流量），`UpdateContextLabel` 用本地估算 `Messages.Sum(m => EstimateTokens(m.Content))` 兜底，保证标题栏有数。

### 8.4 上下文过长弹窗误报修复
**Bug**：`CheckAndAutoCompressAsync` 用 `AppSettings.AutoCompressThreshold`（百分比 0~100，0=禁用）直接和**实际 token 数**比大小（`allTokens < threshold`），导致任意 token 数都触发「上下文过长」弹窗（如模型 262K、用了 48K 也弹）。
**修法**：改为 `占用/模型窗口 >= 阈值百分比` 才弹窗，即 `(double)allTokens / cap * 100 >= threshold`；threshold <= 0 直接跳过。

### 8.5 输入栏外观
- 输入栏外层 Border 改胶囊圆角 `RoundRectangle 30`，背景**全透明** `Transparent`（用户要求"气泡外面要透明的"）。
- 用户气泡/AI 气泡保持圆角矩形 `RoundRectangle 18`（用户强调**输入栏**才是胶囊，聊天气泡不动）。

### 8.6 打包经验补充（本轮反复遇到）
- `XABAA7000`（Zip Renaming Permission denied）与 `MSB6006`（java.exe exit 1）：**连续失败时，先 `dotnet build-server shutdown` 停 build server，再 `rmdir /S /Q` 副本 bin/obj，然后加 `-nodeReuse:false -p:UseSharedCompilation=false` 构建**。单纯清 bin/obj 偶发不彻底。
- 版本号必须用**打包当天**日期：9/16 起打包用 `1.9.16-rc{N}`（csproj `ApplicationDisplayVersion` + `ApplicationVersion` 单调递增，当前 105）。

### 8.7 当前版本（2026-09-20 更新）
- **1.9.18-rc2**（versionCode 105，2026-09-20，含 StorageAccess 真实存储权限）
- APK 位置：`D:\project\QIngYangAI\apk\青阳AI-1.9.18-rc2.apk` 与 `Z:\青阳AI-1.9.18-rc2.apk`
- 历史版本（1.9.16-rc4 等）同 `apk\` 目录可查

---

*青山不改，绿水长流。31号见！👋*
