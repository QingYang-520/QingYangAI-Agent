# 青阳AI v1.9.21 更新公告

> 版本：**1.9.21-rc1**（构建号 106）｜ 更新日期：2026-09-21
> 本版核心：把「完全访问」从假开关接成了真的系统权限

---

## 一句话

上一版的「完全访问」是个**假开关**。这一版把它接上了真的系统权限，Ta 写出来的文件你也终于能看见了。

---

## 🔧 核心修复：「完全访问」不再是个摆设

### 旧版的三个真问题

1. **权限是假的**
   设置里的「完全访问」只是 APP **内部的虚拟开关**——它只决定 APP 自己放不放行，Android 系统层面**根本没授予**「所有文件访问」权限。
   结果：勾了它去写 `/storage/emulated/0`，直接报错。

2. **文件藏在你看不见的地方**
   Ta 生成的文件默认落在 APP 私有沙盒 `/data/user/0/com.qingyang.ai/files/workspace`——**文件管理器里翻不到**，文件像凭空消失了。

3. **权限和工作区没联动**
   就算你手动去系统设置里给了权限，工作区路径也不会跟着变，Ta 还是往沙盒里写。

### 这一版怎么修的

#### ① 「完全访问」现在会真的去要权限

- 勾选「读取全盘」「修改全盘」「完全访问」之前，**先查系统真实权限**
- 没权限 → 弹窗引导 → 一键跳转系统「所有文件访问」页面
- 从系统设置回来会**自动重新校验**
- 没权限时，全盘相关选项**置灰**（半透明）+ 顶部亮出警告条——不再假装能用

#### ② 工作区搬到你能看见的地方

默认工作区改成：

```
内部存储/QingYangAI/WorkSpace
（即 /storage/emulated/0/QingYangAI/WorkSpace）
```

- 文件管理器里**直接可见**，不用再翻沙盒
- 中间任何一级目录不存在，会**自动建好**
- 路径三级回退：**你自己填的路径 → 有全盘权限就用上面这个 → 都没有才退回沙盒**
- Ta 现在会跟你说 `内部存储/Download/xxx.py` 这种**人话路径**，不再甩 `/data/user/0/...` 给你

#### ③ 新增「📤 导出工作区到 Download」

聊天页盾牌面板 + 设置页各有一个按钮，一键把工作区文件复制到公共 `Download` 文件夹，重名自动加 `(1)`，不会覆盖你的文件。

#### ④ 授权之后，老文件不会"消失"

第一次开启全盘权限后，原本落在沙盒里的文件会**自动搬到公共存储**并提示你——同名文件跳过、不覆盖。

---

## 🐛 顺带修掉的两个真 bug

**1. 相对路径会被解析到错误位置**
`notes.txt` 这种相对路径原来会走进程工作目录兜底，而 Android 上工作目录是根目录 `/`，于是它被解析成 `/notes.txt`，文件落到意想不到的地方。已改成**一律按工作区解析**。

**2. 「去开启『所有文件访问』」按钮点了没反应**
按钮的跳转逻辑修好了，现在能正常拉起系统设置页。

---

## 🛡 边界说清楚（免得误会）

- 即使开了全盘权限，**也只能访问公共存储**，读不到其他 App 的私有 `/data` 目录——这是 Android 的沙盒硬限制，任何应用都绕不过去
- Ta 尝试越界时会被**直接拦住**并说明原因，不会再撞异常白费一轮对话

---

## 📌 升级提示

- **覆盖安装即可**，不用卸载（签名没变，聊天记录 / 记忆 / 日记都保留）
- 升级后建议去**聊天页的盾牌图标**里开一次「所有文件访问」——开了之后 Ta 生成的文件才会出现在文件管理器里
- 没开也能正常用，只是文件仍然存在 APP 私有目录里，外面看不到
