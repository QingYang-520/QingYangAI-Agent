# 青阳AI v1.9.7-rc7 更新说明

> 版本号：1.9.7-rc7 · 构建号：75 · 更新日期：2026-09-08

这次更新把「青阳AI」从单纯的聊天工具，升级成了一个会**记得你、感知你、主动关心你**的 AI 陪伴助手：长期记忆、内心生活、生活感知、主动关心、终端执行、日记、多模态……全部在这一个版本里落地。

---

## ✨ 新功能

### 更懂你的记忆与内心
- **长期记忆**：每聊 2 轮，AI 自动从对话中提取关于你的稳定事实（喜好、习惯、重要的人）存入本地，聊天时自然带入，不会重复记录同一条。
- **内心生活**：AI 有自己的心情状态机，对话间隙会"想一想"、更新心情和心里话（你看不到，但会在语气里流露）。
- **后台心跳**：独处时由 AI 自己判断——要不要查一下你的状态、要不要主动开口，不再靠死规则打扰。

### 像真人一样感知你
- **零权限感知**：每次对话自动带上时间、电量、网络、屏幕状态、闹钟、媒体播放等基础信息。
- **深度感知 API**：AI 可通过隐藏指令主动查询 9 个感知接口——屏幕使用时间、当前前台应用、最近通知、今日日程、电量、媒体、闹钟、屏幕、网络（各接口按授权情况返回结果，未授权不报错）。
- **主动关心（可关闭）**：早中晚按时间段默默观察你的状态；三重安静守则兜底（免打扰时段、每日上限 3 条、最小间隔 3 小时），绝不轰炸。
- **通知栏快捷回复**：AI 主动发消息后，不打开 App 也能直接从通知栏回复。

### 更强的工具能力
- **Shizuku 终端**：AI 可通过 `{cmd:}` 指令执行系统命令（查私有 DNS、看存储、查应用信息等），三行气泡实时显示执行状态；高危命令自动拦截 + 5 秒倒计时二次确认。
- **文生图**：说一句"画个图"，AI 通过 `{img:}` 指令生成图片并直接展示在聊天里，图片落盘本地保存。
- **图片理解**：发送图片自动压缩后调用视觉模型识别，识别结果进入对话历史，后续聊天也能引用。
- **AI 自画像**：AI 可以用 `{avatar:}` 指令给自己换头像。

### 语音与多模态
- **TTS 语音朗读**：每条回复可点喇叭朗读；可开启自动朗读（默认关闭，避免每次回复多发请求）。
- **语音输入**：优先使用系统语音识别，失败自动回退听觉模型转写。

### 日记与数据
- **每日日记**：昨天聊得够多，AI 会以第一人称自动写一篇日记；日记页可翻阅，最新一篇会注入对话上下文。
- **数据备份/恢复**：一键把全部聊天、记忆、日记、头像、相伴时间导出为 JSON，换机可完整恢复。
- **相伴天数**：记录第一次相遇的日子，自动计算陪伴天数。

---

## 🎨 体验优化

- **冷启动秒到底**：打开直接定位到最后一屏，不再"从顶部滚下来"。
- **流式输出提速**：网络侧全速收流 + 显示侧逐字上屏（约每秒 50 字），速度与打字手感兼得；切后台被冻结也不容易断。
- **设置即时生效**：修改 API Key / 模型 / 画质后返回聊天页立即生效。
- **缓存命中率显示**：标题栏实时显示 prompt 缓存命中百分比。
- **思考过程可视化**：推理过程可展开/收起，显示思考用时；思考中带流光动画。
- **上下文占用提示**：标题栏显示当前使用上下文 / 模型最大上下文及占用百分比。
- **画质选项**：关闭 / 毛玻璃 / 亚克力 / 液态玻璃。
- **多套模型配置**：配置 1/2/3 自由切换，支持 OpenAI 兼容的任意厂商接口；推理等级（reasoning_effort）由模型能力自动识别，聊天页顶部一键切换。

---

## 🛠 修复

- 旧版单套配置自动迁移到「配置 1」，无需手动重新填写。
- 旧版聊天历史（JSON）自动迁入 SQLite 数据库，原文件备份为 `.bak`。
- 修复设置页修改 Key 后"要再进出一次才生效"的问题。
- 切后台被系统冻结 / 服务端掐线导致的连接中断，自动重试一次，仍失败给出友好提示（不再裸抛 `Socket closed`）。
- 视觉模型 429 限流自动退避重试（最多 3 次）。
- 图片发送前自动压缩（最长边 512px、JPEG 60），避免超限被服务端断开。
- 记忆自动去重，超过 100 条自动清理最旧的。

---

## 📌 已知说明

- **备份不含聊天图片**：图片以文件形式存在设备本地，备份只保留路径，恢复后旧图片不迁移（文字/记忆/日记不受影响）。
- **深度感知需授权**：屏幕使用时间需「使用情况访问权」、通知感知需通知使用权/无障碍/Shizuku 通道之一、日程需日历权限；未授权时对应接口会返回说明文字，不影响聊天。
- **Shizuku 终端**：仅 Android 支持，需安装 Shizuku 并完成授权；AI 执行命令前有安全拦截与确认机制。
- **Windows 桌面端**：感知类能力（屏幕使用时间、前台应用等）仅 Android 支持。
